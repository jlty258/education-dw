# Schema Registry for education-dw

基于 datasource_guard 理念的 Schema 管理。

## 📁 目录结构

```
schemas/
├── README.md
├── templates/
│   └── base_schema.json          # Schema 基础模板
├── students/
│   ├── v1.json                   # 学员 Schema v1
│   └── v2.json                   # 学员 Schema v2（如需要）
├── courses/
│   └── v1.json                   # 课程 Schema v1
├── orders/
│   └── v1.json                   # 订单 Schema v1
└── learning_records/
    └── v1.json                   # 学习记录 Schema v1
```

## 📋 Schema 设计原则

1. **数据源即代码**：Schema 变更走 PR + Review
2. **上游负责**：谁提供数据源，谁写契约
3. **向后兼容**：新字段自动通过，required 字段严格控制
4. **类型宽松**：优先使用 string 类型，提高兼容性

## 🔄 版本管理

- Schema 版本号：v1, v2, v3...
- 每个版本独立文件
- 变更记录在 Git 中追踪

## ✅ 验证流程

1. 提交 Schema PR
2. CI/CD 自动验证（使用 data-diff）
3. 通过后自动合并
4. 失败则阻止合并

